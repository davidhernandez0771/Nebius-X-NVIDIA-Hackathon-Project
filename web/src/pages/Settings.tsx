export default function Settings() {
  return (
    <section>
      <h1>Settings</h1>
      <p>Preferences, usage/cost view and export/delete data aren't built yet (see docs/ARCHITECTURE.md §3).</p>
    </section>
  );
}
